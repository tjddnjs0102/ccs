package org.ccs.app.entrypoints.useraccount.service;

import lombok.AllArgsConstructor;
import org.ccs.app.entrypoints.useraccount.model.UserAccountRequest;
import org.springframework.stereotype.Service;


@Service
@AllArgsConstructor
public class UserAccountService {

    public void signup(UserAccountRequest userAccountRequest) {
        // 회원가입 로직 구현 필요
        // 여기에 userAccountRequest를 사용하여 실제 회원가입 처리 로직을 구현
    }
}

